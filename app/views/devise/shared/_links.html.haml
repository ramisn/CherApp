- resource_class.omniauth_providers.each do |provider|
    = link_to('', omniauth_authorize_path(resource_name, provider, referral_code: @referrer&.referral_code, role: params.dig(:user, :role), professional_role: params.dig(:user, :professional_role), accept_referral_agreement: params.dig(:user, :accept_referral_agreement)), class: "button #{provider}-button social-button")
-# - if controller_name != 'sessions'
-#   = link_to t("devise.links.log_in"), new_session_path(resource_name)
-#   %br/
-# - if devise_mapping.registerable? && controller_name != 'registrations'
-#   = link_to t("devise.links.sign_up"), new_registration_path(resource_name)
-#   %br/
-# - if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
-#   = link_to t("devise.links.forgot_password"), new_password_path(resource_name)
-#   %br/
-# - if devise_mapping.confirmable? && controller_name != 'confirmations'
-# = link_to t("devise.links.confirmation_instructions"), new_confirmation_path(resource_name)
-#   %br/
-# - if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
-#   = link_to t("devise.links.did_not_receive"), new_unlock_path(resource_name)
-#   %br/
