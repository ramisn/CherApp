# == Schema Information
#
# Table name: notification_settings
#
#  id             :bigint           not null, primary key
#  user_id        :bigint
#  accept_email   :boolean          default("true")
#  accept_sms     :boolean          default("true")
#  account        :boolean          default("true")
#  social_network :boolean          default("true")
#  created_at     :datetime         not null
#  updated_at     :datetime         not null
#

# This model initially had no columns defined. If you add columns to the
# model remove the '{}' from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
agent_settings:
  user: agent_user

co_borrower_settings:
  user: co_borrower_user

discarded_user_settings:
  user: discarded_user

co_borrower_user_2_settings:
  user: co_borrower_user_2

user_without_role_settings:
  user: user_without_role

user_without_responses_settings:
  user: user_without_responses

verified_user_settings:
  user: verified_user

test_blocked_user_settings:
  user: test_blocked_user

co_borrower_user_3_settings:
  user: co_borrower_user_3

co_borrower_user_invest_settings:
  user: co_borrower_user_invest
